# Build & test orly on Ubuntu 24.04 / gcc 13.
#
# A fast, build-free `todo-lint` job runs first on every push and pull_request:
# it enforces the TODO convention (no bare /* TODO */ stubs; every TODO
# references a tracked issue as TODO(#nnn) -- see CONTRIBUTING.md).
#
# Four jobs run on every push to master and every pull_request:
#   1. debug-and-test     -- make debug + make test (188 binaries)
#   2. release-build      -- jhm -c release direct + --help smoke on each binary
#   3. lang-tests         -- make debug + python3 tools/lang_test.py
#   4. examples           -- run each examples/* end-to-end with both
#                            its Python and Go drivers (orlyc + orlyi + WS)
#   5. prediction-market-e2e -- the TS-only prediction-market example: its
#                            terminal driver (run-ts.sh) plus a headless-browser
#                            Playwright test driving two tabs trading on one
#                            market (run-e2e.sh)
#
# A seventh job, `asan-smoke`, builds orlyc under AddressSanitizer and runs a
# handful of lang tests through it. Every orlyc run constructs, runs, and
# fully destroys the server (#440 teardown), so this smokes the teardown
# paths -- join-before-destroy, flush, manager destruction -- for
# use-after-free at the memory level. Leak detection is OFF by design:
# teardown deliberately leaks a few pool slabs (docs/teardown-design.md,
# "Deliberate leaks"). Like `tsan`, it skips pull_request events.
#
# A sixth job, `tsan`, builds the concurrency tests under ThreadSanitizer to
# validate the lock-free / commutative-merge claim at the memory level (data-
# race freedom). It is GATING (issue #184): the curated set is clean, so the
# job FAILS on any un-suppressed race. Provably-benign reports are documented
# and suppressed in orly/tsan.supp. Sanitizer builds are slow, so it still
# skips pull_request events and runs on push-to-master, the nightly schedule,
# and manual dispatch. See issues #177 and #184.
#
# Caching: apt packages and the ccache store are cached across runs. The
# ~4.5 GB build-output tree is too large to persist in the 10 GB Actions
# cache, so we cache ccache instead: jhm and orlyc both invoke g++/gcc by
# bare name via execvp, so prepending /usr/lib/ccache to PATH routes every
# compile through ccache. jhm still re-runs jobs (it decides by mtime, and
# checkout resets mtimes), but each compile is a ccache hit -- which also
# speeds lang_test, whose orlyc runs compile generated C++. To guard against
# silent stale-cache bugs, a `schedule:` trigger runs the same jobs daily
# with caching disabled; if the nightly ever diverges from the cached PR
# runs, that's the signal to investigate.
name: ci

on:
  push:
    branches: [master]
  pull_request:
  schedule:
    # Daily at 05:17 UTC -- off-peak, avoids the top-of-hour scheduler rush.
    - cron: '17 5 * * *'
  workflow_dispatch:

jobs:
  todo-lint:
    name: TODO convention lint
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v6

      # Cheap, build-free gate: no bare /* TODO */ stubs, and every TODO
      # references a tracked issue as TODO(#nnn). See CONTRIBUTING.md and
      # tools/maint/lint_todos.py.
      - name: Lint TODO convention
        run: python3 tools/maint/lint_todos.py

  debug-and-test:
    name: make debug + make test
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6

      - name: Install system dependencies
        uses: awalsh128/cache-apt-pkgs-action@v1.6.0
        with:
          packages: build-essential gcc g++ uuid-dev libgmp-dev libaio-dev libsnappy-dev libreadline-dev libboost-system-dev zlib1g-dev bison flex valgrind ccache
          version: 1.2

      - name: Set up ccache
        # jhm and orlyc both invoke `g++`/`gcc` by bare name via execvp. The
        # apt cache-action restores ccache without running its postinst, so
        # the usual /usr/lib/ccache compiler symlinks don't exist -- a prior
        # run showed ccache with 0 reads/writes (never invoked). Create the
        # masquerade symlinks explicitly in /usr/local/bin (ahead of /usr/bin
        # on PATH) so every compile -- the build *and* lang_test's orlyc
        # codegen -- routes through ccache. The store is small/compressible,
        # so unlike the 4.5 GB object tree it persists in the Actions cache.
        run: |
          for c in gcc g++ cc c++; do
            sudo ln -sf "$(command -v ccache)" "/usr/local/bin/$c"
          done
          {
            echo "CCACHE_DIR=$HOME/.ccache"
            echo "CCACHE_MAXSIZE=2G"
            echo "CCACHE_COMPRESS=1"
          } >> "$GITHUB_ENV"

      - name: Cache ccache
        if: github.event_name != 'schedule'
        uses: actions/cache@v5
        with:
          path: ~/.ccache
          key: orly-ccache-${{ runner.os }}-${{ github.job }}-${{ github.sha }}
          restore-keys: |
            orly-ccache-${{ runner.os }}-${{ github.job }}-
            orly-ccache-${{ runner.os }}-

      - name: make debug
        run: |
          ccache --zero-stats
          make debug

      - name: List built binaries
        run: |
          ls -la ../out_orly/debug/orly/orlyc \
                 ../out_orly/debug/orly/server/orlyi \
                 ../out_orly/debug/orly/client/orly_client

      - name: make test
        run: make test

      # Diagnostic: ccache hit/miss for this run's `make debug` (stats were
      # zeroed just before it). High direct-hit rate => the cache is working.
      - name: ccache stats
        if: always()
        run: ccache --show-stats --verbose

  release-build:
    name: jhm -c release
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v6

      - name: Install system dependencies
        uses: awalsh128/cache-apt-pkgs-action@v1.6.0
        with:
          packages: build-essential gcc g++ uuid-dev libgmp-dev libaio-dev libsnappy-dev libreadline-dev libboost-system-dev zlib1g-dev bison flex valgrind ccache
          version: 1.2

      - name: Set up ccache
        # jhm and orlyc both invoke `g++`/`gcc` by bare name via execvp. The
        # apt cache-action restores ccache without running its postinst, so
        # the usual /usr/lib/ccache compiler symlinks don't exist -- a prior
        # run showed ccache with 0 reads/writes (never invoked). Create the
        # masquerade symlinks explicitly in /usr/local/bin (ahead of /usr/bin
        # on PATH) so every compile -- the build *and* lang_test's orlyc
        # codegen -- routes through ccache. The store is small/compressible,
        # so unlike the 4.5 GB object tree it persists in the Actions cache.
        run: |
          for c in gcc g++ cc c++; do
            sudo ln -sf "$(command -v ccache)" "/usr/local/bin/$c"
          done
          {
            echo "CCACHE_DIR=$HOME/.ccache"
            echo "CCACHE_MAXSIZE=2G"
            echo "CCACHE_COMPRESS=1"
          } >> "$GITHUB_ENV"

      - name: Cache ccache
        if: github.event_name != 'schedule'
        uses: actions/cache@v5
        with:
          path: ~/.ccache
          key: orly-ccache-${{ runner.os }}-${{ github.job }}-${{ github.sha }}
          restore-keys: |
            orly-ccache-${{ runner.os }}-${{ github.job }}-
            orly-ccache-${{ runner.os }}-

      # Bootstrap first (same path make debug takes), then drive jhm
      # directly. `make release` works but its Makefile wrapper has a
      # cosmetic broken-pipe quirk at EOF -- direct jhm exits cleanly
      # with the full status trace. Tracked in #10.
      - name: Bootstrap jhm
        run: make bootstrap

      # tools/jhm because the Makefile's `export PATH` only applies inside
      # make's environment. PATH also has to include tools/ so that jhm
      # itself can execvp `make_dep_file` (a sibling in tools/).
      # Mirrors the last line of bootstrap.sh.
      - name: tools/jhm -c release
        run: PATH="$PWD/tools:$PATH" tools/jhm -c release

      - name: Smoke each release binary with --help
        run: |
          ../out_orly/release/orly/client/orly_client --help | head -1
          ../out_orly/release/orly/orlyc --help | head -1
          # orlyi's --help prints a huge memory-tuning header first;
          # asserting non-empty output is enough for a smoke check.
          ../out_orly/release/orly/server/orlyi --help > /tmp/orlyi-help.txt
          test -s /tmp/orlyi-help.txt

  restart-test:
    name: restart durability (tests/restart_test.sh)
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v6

      - name: Install system dependencies
        uses: awalsh128/cache-apt-pkgs-action@v1.6.0
        with:
          packages: build-essential gcc g++ uuid-dev libgmp-dev libaio-dev libsnappy-dev libreadline-dev libboost-system-dev zlib1g-dev bison flex valgrind ccache
          version: 1.2

      - name: Set up ccache
        run: |
          for c in gcc g++ cc c++; do
            sudo ln -sf "$(command -v ccache)" "/usr/local/bin/$c"
          done
          {
            echo "CCACHE_DIR=$HOME/.ccache"
            echo "CCACHE_MAXSIZE=2G"
            echo "CCACHE_COMPRESS=1"
          } >> "$GITHUB_ENV"

      - name: Cache ccache
        if: github.event_name != 'schedule'
        uses: actions/cache@v5
        with:
          path: ~/.ccache
          key: orly-ccache-${{ runner.os }}-${{ github.job }}-${{ github.sha }}
          restore-keys: |
            orly-ccache-${{ runner.os }}-${{ github.job }}-
            orly-ccache-${{ runner.os }}-

      - name: Install python websocket-client
        run: pip3 install --break-system-packages websocket-client

      - name: make debug
        run: make debug

      # Write -> stop -> restart -> read on a real (loopback) disk volume,
      # including installed-package-registry survival (#435).  This is the
      # only job that exercises --create=false, so restart durability can
      # never silently rot again.
      - name: tests/restart_test.sh
        run: ./tests/restart_test.sh

  lang-tests:
    name: lang_test.py
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6

      - name: Install system dependencies
        uses: awalsh128/cache-apt-pkgs-action@v1.6.0
        with:
          packages: build-essential gcc g++ uuid-dev libgmp-dev libaio-dev libsnappy-dev libreadline-dev libboost-system-dev zlib1g-dev bison flex valgrind ccache gdb
          version: 1.3

      - name: Set up ccache
        # jhm and orlyc both invoke `g++`/`gcc` by bare name via execvp. The
        # apt cache-action restores ccache without running its postinst, so
        # the usual /usr/lib/ccache compiler symlinks don't exist -- a prior
        # run showed ccache with 0 reads/writes (never invoked). Create the
        # masquerade symlinks explicitly in /usr/local/bin (ahead of /usr/bin
        # on PATH) so every compile -- the build *and* lang_test's orlyc
        # codegen -- routes through ccache. The store is small/compressible,
        # so unlike the 4.5 GB object tree it persists in the Actions cache.
        run: |
          for c in gcc g++ cc c++; do
            sudo ln -sf "$(command -v ccache)" "/usr/local/bin/$c"
          done
          {
            echo "CCACHE_DIR=$HOME/.ccache"
            echo "CCACHE_MAXSIZE=2G"
            echo "CCACHE_COMPRESS=1"
          } >> "$GITHUB_ENV"

      - name: Cache ccache
        if: github.event_name != 'schedule'
        uses: actions/cache@v5
        with:
          path: ~/.ccache
          key: orly-ccache-${{ runner.os }}-${{ github.job }}-${{ github.sha }}
          restore-keys: |
            orly-ccache-${{ runner.os }}-${{ github.job }}-
            orly-ccache-${{ runner.os }}-

      - name: make debug
        run: make debug

      # Incremental-compilation gate (#312): a second compile of an unchanged
      # package must be a byte-for-byte no-op (no codegen, no gcc), an edit
      # must invalidate the staleness key, and the check-only modes
      # (--syntax-only / --semantic-only / --transient-cc) must emit nothing
      # they promise not to. Needs the stamped orlyc `make debug` just built.
      - name: tests/incremental_test.sh (#312)
        run: ./tests/incremental_test.sh

      # The #463 flake class -- a single test crashing once, deep in a loaded parallel run,
      # never reproducing on rerun -- has only ever yielded anecdotes. Capture cores so the
      # next occurrence yields a stack trace: the runner's default core_pattern pipes to
      # apport, which discards them, so point it at a real directory first.
      - name: Enable core dumps (#463)
        run: |
          sudo mkdir -p /tmp/cores
          sudo chmod 1777 /tmp/cores
          sudo sysctl -w kernel.core_pattern=/tmp/cores/core.%e.%p

      # The #463 class has a hang presentation too (an orlyc that never exits;
      # lang_test waits on it until the job timeout, yielding no evidence).
      # Run every orlyc under `timeout --signal=ABRT`: the whole suite takes
      # ~8 minutes, so any single orlyc alive at 15 is hung, and SIGABRT dumps
      # a core the symbolize step below turns into a backtrace of the deadlock.
      - name: Wrap orlyc in a hang-to-core timeout (#463)
        run: |
          sudo tee /usr/local/bin/orlyc-hang-to-core >/dev/null <<EOF
          #!/bin/sh
          exec timeout --signal=ABRT --kill-after=30 900 "$GITHUB_WORKSPACE/../out_orly/debug/orly/orlyc" "\$@"
          EOF
          sudo chmod +x /usr/local/bin/orlyc-hang-to-core

      - name: python3 tools/lang_test.py
        run: |
          ulimit -c unlimited
          ORLYC=/usr/local/bin/orlyc-hang-to-core \
          python3 tools/lang_test.py -d orly/data tests/lang_tests

      # Symbolize in-job rather than uploading multi-hundred-MB cores: the debug orlyc has
      # full symbols, so the backtrace lands right in the log of the failed run.
      - name: Symbolize any core dumps (#463)
        if: failure()
        run: |
          shopt -s nullglob
          cores=(/tmp/cores/core.*)
          if [ ${#cores[@]} -eq 0 ]; then
            echo "no cores captured (failure was not a crash, or the crash predated core setup)"
            exit 0
          fi
          for core in "${cores[@]}"; do
            echo "================ $core ================"
            gdb -batch -ex 'info threads' -ex 'thread apply all bt' \
                "$GITHUB_WORKSPACE/../out_orly/debug/orly/orlyc" "$core" || true
          done

  examples:
    name: examples/* end-to-end
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6

      - name: Install system dependencies
        uses: awalsh128/cache-apt-pkgs-action@v1.6.0
        with:
          packages: build-essential gcc g++ uuid-dev libgmp-dev libaio-dev libsnappy-dev libreadline-dev libboost-system-dev zlib1g-dev bison flex valgrind ccache
          version: 1.2

      - name: Set up ccache
        # jhm and orlyc both invoke `g++`/`gcc` by bare name via execvp. The
        # apt cache-action restores ccache without running its postinst, so
        # the usual /usr/lib/ccache compiler symlinks don't exist -- a prior
        # run showed ccache with 0 reads/writes (never invoked). Create the
        # masquerade symlinks explicitly in /usr/local/bin (ahead of /usr/bin
        # on PATH) so every compile -- the build *and* lang_test's orlyc
        # codegen -- routes through ccache. The store is small/compressible,
        # so unlike the 4.5 GB object tree it persists in the Actions cache.
        run: |
          for c in gcc g++ cc c++; do
            sudo ln -sf "$(command -v ccache)" "/usr/local/bin/$c"
          done
          {
            echo "CCACHE_DIR=$HOME/.ccache"
            echo "CCACHE_MAXSIZE=2G"
            echo "CCACHE_COMPRESS=1"
          } >> "$GITHUB_ENV"

      - name: Install python websocket-client
        run: pip3 install --break-system-packages websocket-client

      - name: Install Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'

      - name: Cache ccache
        if: github.event_name != 'schedule'
        uses: actions/cache@v5
        with:
          path: ~/.ccache
          key: orly-ccache-${{ runner.os }}-${{ github.job }}-${{ github.sha }}
          restore-keys: |
            orly-ccache-${{ runner.os }}-${{ github.job }}-
            orly-ccache-${{ runner.os }}-

      - name: make debug
        run: make debug
        id: build

      # Smoke-tests the orlyc + orlyi + WebSocket protocol end to end.
      # Both drivers (Python + Go) exercise the same scenario and
      # self-check the full 72-value balance trajectory across both
      # branches; any divergence fails CI.
      - name: run.sh (python driver)
        run: ../retry-on-ws-timeout.sh ./run.sh
        if: ${{ !cancelled() && steps.build.outcome == 'success' }}
        working-directory: examples/bitcoin-time-travel

      - name: run-go.sh (go driver)
        run: ../retry-on-ws-timeout.sh ./run-go.sh
        if: ${{ !cancelled() && steps.build.outcome == 'success' }}
        working-directory: examples/bitcoin-time-travel

      # Same scenario as bitcoin-time-travel but uses set union instead
      # of integer addition. Demonstrates the polymorphic-monoid claim
      # from that demo's README: same fold pattern, different operator.
      - name: wikipedia-categories run.sh (python driver)
        run: ../retry-on-ws-timeout.sh ./run.sh
        if: ${{ !cancelled() && steps.build.outcome == 'success' }}
        working-directory: examples/wikipedia-categories

      - name: wikipedia-categories run-go.sh (go driver)
        run: ../retry-on-ws-timeout.sh ./run-go.sh
        if: ${{ !cancelled() && steps.build.outcome == 'success' }}
        working-directory: examples/wikipedia-categories

      # The concurrent-writers demo: N sessions all `+= n` into the same
      # hot keys. Self-checks that every increment lands (no lost updates).
      # This is the integration validation for the issue #49 fix --
      # without phases 2+3, only ~1/N of the writes survive.
      # DEMO_SCALE=small opts into the CI-tractable 4-writer / 800-event
      # workload; the default (large) is the 8-writer / 2000-event
      # local-showcase mode used in the README / tweet captures.
      - name: wikipedia-pageviews run.sh (python driver, concurrent +=)
        run: ../retry-on-ws-timeout.sh ./run.sh
        if: ${{ !cancelled() && steps.build.outcome == 'success' }}
        working-directory: examples/wikipedia-pageviews
        env:
          DEMO_SCALE: small

      - name: wikipedia-pageviews run-go.sh (go driver, concurrent +=)
        run: ../retry-on-ws-timeout.sh ./run-go.sh
        if: ${{ !cancelled() && steps.build.outcome == 'success' }}
        working-directory: examples/wikipedia-pageviews
        env:
          DEMO_SCALE: small

      # Multi-agent knowledge-graph demo: N concurrent agents each
      # extract from a disjoint slice of a corpus, writing tags
      # (|= set-union), per-doc mentions (+= int), and per-pair
      # cooccurrences (+= int) into one shared POV with zero
      # coordination. Combines both commutative-write shapes from the
      # demos above and runs them concurrently -- the shape multi-agent
      # LLM-extraction pipelines need. DEMO_SCALE=small uses 4 agents
      # over 20 docs for CI tractability.
      - name: agent-swarm run.sh (python driver, concurrent |= and +=)
        run: ../retry-on-ws-timeout.sh ./run.sh
        if: ${{ !cancelled() && steps.build.outcome == 'success' }}
        working-directory: examples/agent-swarm
        env:
          DEMO_SCALE: small

      - name: agent-swarm run-go.sh (go driver, concurrent |= and +=)
        run: ../retry-on-ws-timeout.sh ./run-go.sh
        if: ${{ !cancelled() && steps.build.outcome == 'success' }}
        working-directory: examples/agent-swarm
        env:
          DEMO_SCALE: small

      # GRC-20 reference demo: each editor's op is one set-union into a
      # per-(entity, property) history set; reads replay the timestamp-
      # sorted log to reconstruct entity state (or any historical
      # state). Three phases: wiki biographical / stanford schools +
      # relations / concurrent race on the same attribute. Self-checks
      # that both race events land in history.
      - name: grc20-pov run.sh (python driver, event-sourced KG)
        run: ../retry-on-ws-timeout.sh ./run.sh
        if: ${{ !cancelled() && steps.build.outcome == 'success' }}
        working-directory: examples/grc20-pov

      - name: grc20-pov run-go.sh (go driver, event-sourced KG)
        run: ../retry-on-ws-timeout.sh ./run-go.sh
        if: ${{ !cancelled() && steps.build.outcome == 'success' }}
        working-directory: examples/grc20-pov

      # Collaborative text CRDT (Logoot): two editors stream inserts/deletes
      # into one shared POV with no coordination; each character is one
      # set-union with a dense position id, and the visible text is the
      # engine's sort+fold. Four phases (sequential / non-overlapping /
      # same-spot race / delete+insert). Self-checks convergence across three
      # independent readers, no lost concurrent edits, and stable time-travel.
      - name: crdt-text run.sh (python driver, collaborative CRDT)
        run: ../retry-on-ws-timeout.sh ./run.sh
        if: ${{ !cancelled() && steps.build.outcome == 'success' }}
        working-directory: examples/crdt-text

      - name: crdt-text run-go.sh (go driver, collaborative CRDT)
        run: ../retry-on-ws-timeout.sh ./run-go.sh
        if: ${{ !cancelled() && steps.build.outcome == 'success' }}
        working-directory: examples/crdt-text

      # Recursive sum-type storage + client marshaling smoke test (issue
      # #115): store a fixed recursive value of each shape (self-recursive
      # tree, container-of-self doc, mutual tree/forest, nested-variant tree),
      # read it back, and self-check the marshaled JSON. Guards the
      # dynamic-var/WS marshaling path that the C++ lang_test (a native-read
      # harness) cannot reach.
      - name: recursive-variants run.sh (python driver, recursive storage)
        run: ../retry-on-ws-timeout.sh ./run.sh
        if: ${{ !cancelled() && steps.build.outcome == 'success' }}
        working-directory: examples/recursive-variants

      - name: recursive-variants run-go.sh (go driver, recursive storage)
        run: ../retry-on-ws-timeout.sh ./run-go.sh
        if: ${{ !cancelled() && steps.build.outcome == 'success' }}
        working-directory: examples/recursive-variants

  prediction-market-e2e:
    name: prediction-market headless browser (Playwright)
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6

      - name: Install system dependencies
        uses: awalsh128/cache-apt-pkgs-action@v1.6.0
        with:
          packages: build-essential gcc g++ uuid-dev libgmp-dev libaio-dev libsnappy-dev libreadline-dev libboost-system-dev zlib1g-dev bison flex valgrind ccache
          version: 1.2

      - name: Set up ccache
        # Same masquerade-symlink trick as the examples job: route every compile
        # through ccache so the build (and orlyc codegen) hit the cache.
        run: |
          for c in gcc g++ cc c++; do
            sudo ln -sf "$(command -v ccache)" "/usr/local/bin/$c"
          done
          {
            echo "CCACHE_DIR=$HOME/.ccache"
            echo "CCACHE_MAXSIZE=2G"
            echo "CCACHE_COMPRESS=1"
          } >> "$GITHUB_ENV"

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Cache ccache
        if: github.event_name != 'schedule'
        uses: actions/cache@v5
        with:
          path: ~/.ccache
          key: orly-ccache-${{ runner.os }}-${{ github.job }}-${{ github.sha }}
          restore-keys: |
            orly-ccache-${{ runner.os }}-${{ github.job }}-
            orly-ccache-${{ runner.os }}-

      - name: make debug
        run: make debug
        id: build

      # Terminal driver (orly TS client): concurrent traders on shared per-round
      # keys via Promise.all, time-travel price history, resolution + payout, all
      # self-checked against an independently computed ground truth.
      - name: run-ts.sh (terminal driver, self-check)
        run: ./run-ts.sh
        if: ${{ !cancelled() && steps.build.outcome == 'success' }}
        working-directory: examples/prediction-market

      # The browser's @playwright/test + a headless Chromium with its system
      # libs (libnss3/libnspr4/libasound2, ...). --with-deps installs those.
      - name: Install Playwright Chromium
        run: npm install && npx playwright install --with-deps chromium
        if: ${{ !cancelled() && steps.build.outcome == 'success' }}
        working-directory: examples/prediction-market/web

      # Headless browser gate: two browser contexts on the same #pov= link both
      # bet (one YES, one NO) concurrently; the test asserts the pool reaches $20
      # and the price moves to 50/50 live in both -- the two-tab story, automated.
      - name: run-e2e.sh (headless browser, two-tab concurrency)
        run: ./run-e2e.sh
        if: ${{ !cancelled() && steps.build.outcome == 'success' }}
        working-directory: examples/prediction-market/web

      # MCP server smoke (#526): an MCP stdio client spawns clients/mcp's
      # server and exercises every tool (install / new_pov / call / call_batch
      # / error surface) against a live mem-sim orlyi. Rides this job for the
      # already-built tree + Node toolchain.
      - name: MCP server smoke (clients/mcp)
        run: bash smoke/run-smoke.sh
        if: ${{ !cancelled() && steps.build.outcome == 'success' }}
        working-directory: clients/mcp

  tsan:
    name: ThreadSanitizer
    # GATING (issue #184): the curated concurrency set is clean under TSan, so
    # this job now FAILS if any un-suppressed ThreadSanitizer warning appears
    # (see the "fail on any race" check below). Provably-benign reports are
    # documented and suppressed in orly/tsan.supp, never ignored here.
    # Sanitizer builds are slow, so we still skip pull_request events to keep PR
    # latency low; the gate runs on push-to-master, the nightly schedule, and
    # manual dispatch (so a race that lands reds master immediately). Extending
    # the gate to pull_request events is a possible future tightening.
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v6

      - name: Install system dependencies
        uses: awalsh128/cache-apt-pkgs-action@v1.6.0
        with:
          packages: build-essential gcc g++ uuid-dev libgmp-dev libaio-dev libsnappy-dev libreadline-dev libboost-system-dev zlib1g-dev bison flex valgrind ccache
          version: 1.2

      - name: Set up ccache
        # Same masquerade as the other jobs: jhm/orlyc invoke g++/gcc by bare
        # name via execvp, so symlink them ahead of /usr/bin to route compiles
        # through ccache. TSan objects are large, hence the bigger ceiling.
        run: |
          for c in gcc g++ cc c++; do
            sudo ln -sf "$(command -v ccache)" "/usr/local/bin/$c"
          done
          {
            echo "CCACHE_DIR=$HOME/.ccache"
            echo "CCACHE_MAXSIZE=3G"
            echo "CCACHE_COMPRESS=1"
          } >> "$GITHUB_ENV"

      - name: Cache ccache
        # Cache on every trigger (including schedule): unlike the stale-cache
        # canary the other jobs run nightly, this job's value is the race
        # report, not cache-correctness, and a cold TSan build is expensive.
        uses: actions/cache@v5
        with:
          path: ~/.ccache
          key: orly-ccache-${{ runner.os }}-${{ github.job }}-${{ github.sha }}
          restore-keys: |
            orly-ccache-${{ runner.os }}-${{ github.job }}-
            orly-ccache-${{ runner.os }}-

      # ThreadSanitizer's shadow memory needs lower ASLR entropy than modern
      # kernels default to; otherwise libtsan aborts at startup with
      # "FATAL: ThreadSanitizer: unexpected memory mapping". Lower it for the
      # job, and additionally run each binary under `setarch -R` (no per-process
      # randomization) as belt-and-suspenders.
      - name: Lower ASLR entropy for TSan
        run: sudo sysctl -w vm.mmap_rnd_bits=28

      - name: Bootstrap jhm
        run: make bootstrap

      - name: Build & run concurrency tests under TSan
        run: |
          set -u
          # Concurrency-focused targets: the synchronization primitive, the
          # fiber scheduler and lock pools, the Tetris promotion logic, and the
          # cross-repo / fold merge tests the lock-free claim rests on.
          TESTS="
          base/scheduler.test
          base/event_semaphore.test
          base/pump.test
          base/thread_local_global_pool.test
          base/thread_local_registered_pool.test
          orly/indy/fiber/fiber.test
          orly/indy/fiber/fiber_lock.test
          orly/indy/fiber/algorithm.test
          orly/indy/fiber/jump_runnable.test
          orly/indy/fiber/sem.test
          orly/indy/disk/result.test
          orly/indy/disk/util/volume_manager.test
          orly/indy/disk/durable_manager.test
          orly/indy/context_fold.test
          orly/indy/context_xrepo.test
          orly/server/tetris_manager.test
          "
          PATH="$PWD/tools:$PATH" tools/jhm -c tsan $TESTS

          mkdir -p tsan-logs
          total=0
          line="----------------------------------------------------------------"
          fmt='%-48s %s\n'
          printf "$fmt" "TEST" "TSAN WARNINGS"
          echo "$line"
          for t in $TESTS; do
            bin="../out_orly/tsan/$t"
            log="tsan-logs/$(echo "$t" | tr / _).log"
            # Per-test wall-clock guard: a real deadlock under TSan would
            # otherwise hang the job to its timeout. exitcode=0 so a race does
            # not itself fail the run -- we count warnings from the logs.
            timeout 600 setarch "$(uname -m)" -R \
              env TSAN_OPTIONS="halt_on_error=0 exitcode=0 history_size=4 suppressions=$PWD/orly/tsan.supp" \
              "$bin" >"$log" 2>&1 || true
            n=$(grep -c "WARNING: ThreadSanitizer" "$log" || true)
            total=$((total + n))
            printf "$fmt" "$t" "$n"
          done
          echo "$line"
          echo "Total ThreadSanitizer warnings: $total"
          echo
          echo "Full reports are uploaded as the 'tsan-logs' artifact."
          # GATING (#184): the curated set is clean, so any un-suppressed race is
          # a regression. Provably-benign reports are documented + suppressed in
          # orly/tsan.supp (applied via TSAN_OPTIONS suppressions= above), so a
          # non-zero count here is a real, untriaged data race -> fail the job.
          if [ "$total" -ne 0 ]; then
            echo "::error::$total un-suppressed ThreadSanitizer warning(s) -- a data-race regression. See the tsan-logs artifact; if provably benign, document and add a suppression to orly/tsan.supp."
            exit 1
          fi
          echo "No un-suppressed ThreadSanitizer warnings."

      - name: Upload TSan reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: tsan-logs
          path: tsan-logs/
          if-no-files-found: warn

  asan-smoke:
    name: AddressSanitizer smoke
    # Smokes the #440 teardown paths: every orlyc run constructs, runs, and
    # fully destroys the server, so a handful of lang tests under ASan police
    # Shutdown()/~TServer for use-after-free and heap corruption. Sanitizer
    # builds are slow, so (like `tsan`) this skips pull_request events and
    # runs on push-to-master, the nightly schedule, and manual dispatch.
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v6

      - name: Install system dependencies
        uses: awalsh128/cache-apt-pkgs-action@v1.6.0
        with:
          packages: build-essential gcc g++ uuid-dev libgmp-dev libaio-dev libsnappy-dev libreadline-dev libboost-system-dev zlib1g-dev bison flex valgrind ccache
          version: 1.2

      - name: Set up ccache
        # Same masquerade as the other jobs: jhm/orlyc invoke g++/gcc by bare
        # name via execvp, so symlink them ahead of /usr/bin to route compiles
        # through ccache.
        run: |
          for c in gcc g++ cc c++; do
            sudo ln -sf "$(command -v ccache)" "/usr/local/bin/$c"
          done
          {
            echo "CCACHE_DIR=$HOME/.ccache"
            echo "CCACHE_MAXSIZE=3G"
            echo "CCACHE_COMPRESS=1"
          } >> "$GITHUB_ENV"

      - name: Cache ccache
        # Cache on every trigger (like `tsan`): this job's value is the ASan
        # report, not cache-correctness, and a cold sanitizer build is
        # expensive.
        uses: actions/cache@v5
        with:
          path: ~/.ccache
          key: orly-ccache-${{ runner.os }}-${{ github.job }}-${{ github.sha }}
          restore-keys: |
            orly-ccache-${{ runner.os }}-${{ github.job }}-
            orly-ccache-${{ runner.os }}-

      - name: Bootstrap jhm
        run: make bootstrap

      - name: Build orlyc under ASan
        run: PATH="$PWD/tools:$PATH" tools/jhm -c asan orly/orlyc

      - name: Run teardown smoke under ASan
        # Three lang tests through the ASan orlyc: two with with-block writes
        # (they exercise the dirty-self-pin release and the full manager
        # destruction) and one pure. An ASan error makes orlyc exit non-zero,
        # lang_test reports it as an unexpected failure, and its exit code
        # fails the job. detect_leaks=0 is deliberate: teardown intentionally
        # leaks a few pool slabs (docs/teardown-design.md, "Deliberate
        # leaks"); this smoke gates on use-after-free, not leak accounting.
        run: |
          ASAN_OPTIONS=detect_leaks=0 \
          ORLYC="$PWD/../out_orly/asan/orly/orlyc" \
          PATH="$PWD/tools:$PATH" \
          python3 tools/lang_test.py \
            tests/lang_tests/general/assertions.orly \
            tests/lang_tests/general/exists.orly \
            tests/lang_tests/general/arithmetic.orly
