- **Fixed**: when `orlyc`'s generated-C++ compile fails, the g++ diagnostics are now printed unconditionally instead of being hidden behind `-d` — an environment-shaped failure (missing system header, bad `ORLY_SRC_ROOT`, toolchain drift) previously dead-ended at a generic "see a team member" message. The generated sources are kept in the output directory on failure for inspection (#533).
