- **Added**: `examples/mcp-agent-duet` — two independent agent processes, each speaking only MCP through its own `orly-mcp` server, build one knowledge graph in one shared POV concurrently (reusing `agent-swarm`'s schema). The CI-gated verifier checks tag provenance (corroborated tags keep one record per agent), every counter, and a `reach()` traversal that bridges the two agents' contributions; the README documents running the same topology with live agent sessions (#528).
