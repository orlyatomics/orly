- **Changed**: the `orly-repl` `:help` now teaches the commutative field calls — `*<['hits']>::(int) += 1` and `*<['tags']>::({str}) |= {'blue'}` — with the leading `*` and `::(type)` annotation that the form requires, and a note that `+=` on an absent key folds from the monoid identity so there is nothing to create first. The help previously showed only assignment (`new <['k']> <- 1`) and read, so the one operation the project is pitched on was undiscoverable from the prompt: substituting `+=` into the documented write form fails with an internal compiler path (`orly/type/add_visitor.h, 50`) and the follow-up read returns `0`, which reads as a silently wrong write rather than a syntax error (#548).
- **Changed**: the README leads with the use case rather than the architecture. The concurrent-writer problem and a copy-pasteable 30-second REPL demo (both operators, with their real output) come first; the MCP server is promoted from a parenthetical to a first-class quickstart with a working `npx -y orly-mcp` client config; the 2019-dormancy status paragraph moves from above the fold to its own `## Project status` section near the end. The Docker section now states that the published image is `linux/amd64` and runs under emulation on Apple Silicon rather than implying it is native on "any host" (#548).
