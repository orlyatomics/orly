- **Changed**: `import_replication.test` no longer dereferences RPC futures untimed: every `Try`/install/import/pov call now waits on the future's event fd with a deadline (240s) and fails the test with the RPC's name and the server log tails if no answer comes — the #537 wedge's client half. A recurrence in CI now reads as `RPC [write_val Try] unanswered after 240s` instead of a hung test (#537).
