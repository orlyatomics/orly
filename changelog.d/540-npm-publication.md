- **Added**: the TypeScript stack is published to npm — [`@orlyatomics/orly`](https://www.npmjs.com/package/@orlyatomics/orly) (the driver; the bare name `orly` was taken), [`orly-mcp`](https://www.npmjs.com/package/orly-mcp) (`npx -y orly-mcp` in an MCP config), and [`orly-repl`](https://www.npmjs.com/package/orly-repl). In-repo code keeps the `orly` alias for the driver (`"orly": "file:../ts"`); `tools/npm_publish.sh` stages, rewrites the alias to the registry name, builds, and publishes in dependency order (#540).
